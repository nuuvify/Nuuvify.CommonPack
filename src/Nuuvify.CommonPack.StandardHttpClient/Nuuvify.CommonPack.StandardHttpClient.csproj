<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <!-- Project Specific Configuration -->
        <RootNamespace>Nuuvify.CommonPack.StandardHttpClient</RootNamespace>
        <AssemblyName>Nuuvify.CommonPack.StandardHttpClient</AssemblyName>
        <Product>Nuuvify CommonPack StandardHttpClient</Product>

        <!-- Package Specific Metadata -->
        <PackageId>Nuuvify.CommonPack.StandardHttpClient</PackageId>
        <Title>Nuuvify CommonPack StandardHttpClient</Title>
        <Description>Cliente HTTP otimizado para .NET com gerenciamento automctico de tokens, retry policies com Polly, resource management otimizado e performance aprimorada. Inclui ConfigureAwait(false) para bibliotecas, proper disposal patterns e prevencco de memory leaks. Ideal para comunicacco resiliente com APIs REST e web services SOAP.</Description>
        <Summary>Cliente HTTP otimizado com retry policies, gerenciamento de tokens e resource management para .NET 8.0</Summary>

        <!-- Package Specific Tags -->
        <PackageTags>http;httpclient;api;rest;polly;retry;circuit-breaker;token;authentication;performance;async;configureawait;resource-management;memory-leak-prevention;nuuvify;dotnet;netcore;aspnetcore</PackageTags>
    </PropertyGroup>

    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App" />
        <PackageReference Include="Microsoft.Extensions.Http.Polly" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Nuuvify.CommonPack.Security.Abstraction\Nuuvify.CommonPack.Security.Abstraction.csproj" />
    </ItemGroup>

</Project>
